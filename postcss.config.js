// PostCSS config
// Tailwind CSS uses PostCSS as its build pipeline.
// Autoprefixer adds vendor prefixes automatically for cross-browser support.
export default {
  plugins: {
    tailwindcss:  {},
    autoprefixer: {},
  },
}